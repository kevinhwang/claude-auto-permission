-- case.yaml --
description: Pipe between safe commands is allowed.
tool: Bash
expected:
  combined: allow
-- tool_input.json --
{"command":"git log --oneline | head -20 | grep fix"}
-- config.txtpb --
projects {
  path_patterns: "/**"
  static_bash_rules {
    use_default_rules {}
  }
}
